# Documentación de la API

## Endpoints

- `GET /` — bienvenida
- `GET /health` — estado del servicio
- `POST /login` — autenticación

## Instalación

```bash
uv sync
uvicorn app:app --reload
```
